service: nestjs-lambda

frameworkVersion: '3'

plugins:
  - serverless-jetpack

provider:
  name: aws
  runtime: nodejs14.x
  region: us-east-2

functions:
  app:
    handler: dist/main.handler
    events:
      - http:
          method: any
          path: /{proxy+}